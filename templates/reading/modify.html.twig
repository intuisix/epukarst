{% extends 'base.html.twig' %}

{% block title %}
    Modifier le relevé {{reading.code}}
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier le relevé {{reading.code}}</h1>

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Enregistrer la modification</button>

        {{ form_end(form) }}
    </div>
{% endblock %}
