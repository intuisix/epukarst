{% set user = app.user %}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('home') }}">EpuKarst</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('system') }}">Systèmes</a>
            </li>
            {% if user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('reading') }}">Relevés</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('instrument') }}">Instruments</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('parameter') }}">Paramètres</a>
                </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{user.picture}}" class="avatar avatar-mini" alt="Avatar de {{user.displayName}}"/> {{user.displayName}}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('user_logout') }}" class="dropdown-item">Vous déconnecter</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('user_login') }}" class="nav-link">Vous connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
